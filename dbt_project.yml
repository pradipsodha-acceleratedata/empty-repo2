name: 'apline2'
version: '1.0.0'
config-version: 2

profile: 'apline2'

model-paths: ["models"]
seed-paths: ["seeds"]
test-paths: ["tests"]
analysis-paths: ["analyses"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  apline2:
    +materialized: table
